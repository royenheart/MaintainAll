# tmux

## 自动更新 tmux

为了能更好拷贝内容，推荐使用 tmux 3.3+ 版本。加载配置后使用 prefix + U 后台编译安装最新版本 tmux 至 ~/.local/tmux。

## MacOS 终端无法复制

需要开启 OSC 52：

1. 如果是 iterm，settings -> general -> selection -> Applications in terminal may access clipboard 开启即可
