# docker-compose.yml
services:
  web:
    image: githubyumao/mcsmanager-web:latest
    restart: unless-stopped
    ports:
      - "23333:23333"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${HOME}/softwares/mcsm/web/data:/opt/mcsmanager/web/data
      - ${HOME}/softwares/mcsm/web/logs:/opt/mcsmanager/web/logs

  daemon:
    image: githubyumao/mcsmanager-daemon:latest
    restart: unless-stopped
    ports:
      - "24444:24444"
    environment:
      - MCSM_DOCKER_WORKSPACE_PATH=${HOME}/softwares/mcsm/daemon/data/InstanceData
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${HOME}/softwares/mcsm/daemon/data:/opt/mcsmanager/daemon/data
      - ${HOME}/softwares/mcsm/daemon/logs:/opt/mcsmanager/daemon/logs
      - /var/run/docker.sock:/var/run/docker.sock
