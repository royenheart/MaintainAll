upstream {
  alidns: 'udp://223.5.5.5:53'
  # Use IP form so DNS upstream itself does not depend on resolving dns.google
  # through an already-failing proxy dialer.
  googledns: 'tcp+udp://8.8.8.8:53'
}
routing {
  request {
    qname(geosite:cn) -> alidns
    fallback: googledns
  }
}
